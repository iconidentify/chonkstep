Normal v2 copies the original immutable normal capture benchmark plus its immutable startup dependency. The only change is atomic diagnostic screenshot marker publication outside measured phase windows. All key/motion loops, delays, barriers, process sampling, and allocation queries are byte-for-byte unchanged. Use for separate allocation diagnostic runs only; prior normal timing evidence stays preserved against the original harness.
