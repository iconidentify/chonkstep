Mixed v2 preserves mixed-v1 and changes only diagnostic screenshot marker publication: write the full output path to a private sibling staging file, then atomically replace the marker. The marker is polled by the compositor; direct write_text exposes an empty file between truncate and write. This diagnostic occurs outside the timed phase and the correction changes no phase, timing loop, client, workload payload, or deadline. Completed v1 calibration and prior ordinary evidence remain preserved. Both matched pressure binaries use exactly v2.
