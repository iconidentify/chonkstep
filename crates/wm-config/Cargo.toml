[package]
name = "wm-config"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "User configuration: keybindings, workspace count, default focus policy."

[dependencies]
wm-core = { path = "../wm-core" }
serde = { version = "1", features = ["derive"] }
