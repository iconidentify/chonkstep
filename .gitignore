/target

# Agent worktrees and scratch state the Claude Code harness creates in
# the checkout. Never part of the project.
/.claude/worktrees/
__pycache__/
*.pyc

# The out-of-workspace reproducer builds into its own target directory.
/examples/layer-role-repro/target/
